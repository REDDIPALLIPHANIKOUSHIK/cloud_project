# Flask API

Install with `python -m pip install -r requirements.txt`. Train the pipeline with `python train_model.py` (downloads the documented public heart.csv on first run). Start locally with `python app.py`; production uses Gunicorn. Set `CORS_ORIGINS` to a comma-separated list of exact frontend origins. See [API documentation](../docs/api.md).

